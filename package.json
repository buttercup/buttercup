{
    "name": "Buttercup Password Manager",
    "private": true,
    "workspaces": [
        "applications/*",
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "build:packages": "npm run --workspace=packages/** build",
        "clean": "npm run clean --workspaces"
    },
    "license": "GPL-3.0",
    "devDependencies": {
        "@types/node": "^22.7.4",
        "rimraf": "^6.0.1",
        "typescript": "^5.6.2"
    }
}
